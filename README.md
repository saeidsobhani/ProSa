# ProSa — Circular Economy Project
```markdown
# ProSa — Circular Economy (static demo)

An interactive static front-end that visualizes participants in a circular economy.

To start: open `index.html` in your browser.

Optional (serve locally):

```bash
python3 -m http.server 8000
# then open http://localhost:8000
```
